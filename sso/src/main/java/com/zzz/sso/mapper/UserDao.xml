<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzz.sso.dao.UserDao">
    <select id="selectUser" parameterType="com.zzz.sso.bean.User" resultMap="user">
        SELECT
          userid,
          username,
          password,
          to_char(registerDate,'yyyymmdd hh24:mi:ss') as registerDate,
          to_char(updateDate,'yyyymmdd hh24:mi:ss') as updateDate,
          isvalid
        FROM
          SYSTEM_USER
        WHERE
          1 = 1
          <if test="username != null">
              username = #{username}
          </if>
          <if test="password != null">
              password = #{password}
          </if>
          <if test="isvalid != null">
              isvalid = #{isValid}
          </if>
    </select>

</mapper>